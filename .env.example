ANTHROPIC_API_KEY=
PORT=3000

# Optional — misuse alerts (jailbreak / evasion attempts) email via Gmail SMTP.
# Leave blank to just log alerts to the console instead.
ALERT_EMAIL_FROM=
ALERT_EMAIL_PASSWORD=
ALERT_EMAIL_TO=

# Optional — separate destination for legitimate "forward to Expert Service" requests.
# Deliberately not the same inbox as the misuse alerts above. Leave blank to disable.
EXPERT_SERVICE_EMAIL_TO=
